# Frontend API base URL (no trailing slash)
# Leave empty for local dev — the Vite proxy handles /api/* → Laravel automatically.
# In production, set this to your Laravel backend origin so requests route correctly.
# Example: VITE_API_URL=https://api.murjanlab.my.id
VITE_API_URL=
